<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="res_config_settings_view_form_purchase" model="ir.ui.view">
            <field name="name">res.config.settings.view.form.inherit.purchase</field>
            <field name="model">res.config.settings</field>
            <field name="priority" eval="26"/>
            <field name="inherit_id"
                   ref="base.res_config_settings_view_form"/>
            <field name="arch" type="xml">

                <xpath expr="//form" position="inside">
                    <app data-string="Purchase Requests"
                         string="Purchase Requests"
                         name="purchase_request"
                         groups="atta_purchase_request.purchase_request_group_manager">
                        <block title="Purchase Request" name="purchase_request_settings">
                            <setting id="has_pr_4th_approve_setting"
                                     string="4-Level Approval"
                                     help="Approve purchase requests by 4 levels. The 4th level is triggered by the untaxed amount of the purchase request.">
                                <field name="has_pr_4th_approve"/>
                                <div class="content-group mt8" invisible="not has_pr_4th_approve">
                                    <div class="row">
                                        <label for="pr_4th_approve_amount" class="col-md-4 o_light_label"/>
                                        <field name="pr_4th_approve_amount" required="has_pr_4th_approve"/>
                                    </div>
                                    <div class="text-muted">
                                        Minimum untaxed amount that triggers the 4th approval level.
                                    </div>
                                </div>
                            </setting>
                            <setting id="is_required_vendor_in_pr_setting"
                                     string="Require Preferred Vendors"
                                     help="Require preferred vendors for all PR lines in 2nd approval.">
                                <field name="is_required_vendor_in_pr"/>
                            </setting>
                        </block>
                    </app>
                </xpath>

            </field>
        </record>

        <record id="purchase_request_action_configuration"
                model="ir.actions.act_window">
            <field name="name">Settings</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.config.settings</field>
            <field name="view_mode">form</field>
            <field name="target">current</field>
            <field name="context">{
                    'module' : 'purchase_request',
                    'bin_size': False,
                }
            </field>
        </record>

    </data>
</odoo>
